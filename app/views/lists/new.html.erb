<h1>New List</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for list:</p>
    <ul>
      <li>Make a title for the list.</li>
      <li>The Title must be at least 2 characters long</li>
      <li>Make sure the title of the list is unique.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @list do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter list title" %>
      </div>
      <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>